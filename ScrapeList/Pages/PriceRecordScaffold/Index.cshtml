@page
@model ScrapeList.Pages.PriceRecordScaffold.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PriceRecord[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PriceRecord[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PriceRecord[0].Material)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PriceRecord) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Material.MaterialName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PriceRecordID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PriceRecordID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PriceRecordID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
