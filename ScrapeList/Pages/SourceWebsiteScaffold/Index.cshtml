@page
@model ScrapeList.Pages.SourceWebsiteScaffold.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SourceWebsite[0].URL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SourceWebsite[0].xPathPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SourceWebsite) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.URL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.xPathPrice)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WebsiteID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WebsiteID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WebsiteID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
